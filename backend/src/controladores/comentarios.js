var comentarios = require('../modelos/comentarios');
var fs = require('fs');
var path = require('path');


async function recuperarTodos(req, res) {
    try {
        //Await
    } catch (error) {

    }
}

async function añadirNuevo(req, res) {
    try {
        //Await
    } catch (error) {

    }
}

async function eliminar(req, res) {
    try {
        //Await
    } catch (error) {

    }
}

module.exports = { recuperarTodos, añadirNuevo, eliminar };